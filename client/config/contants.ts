const contants = {
    LOCATION: process.env.NEXT_PUBLIC_URL_SDN,
    MAPS: process.env.NEXT_PUBLIC_URL_SDN,
    USER: {
        GENDER: {
            FEMALE: 1,
            MALE: 2,
        }
    }
}

export default contants