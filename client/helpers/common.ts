export const setStateForm = (tabObj: any, state): void =>{
    console.log(tabObj.target.type, state)
    switch (tabObj.target.type) {
        case 'text':
            break;
    
        default:
            break;
    }
}